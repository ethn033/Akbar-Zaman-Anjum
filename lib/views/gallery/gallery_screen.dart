import 'package:flutter/material.dart';

class AuthorGalleryScreen extends StatelessWidget {
  const AuthorGalleryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('ګالری')),
      body: Container(
        child: Center(
          child: Text('We are working on this..'),
        ),
      ),
    );
  }
}
